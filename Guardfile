# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'haml', :input => 'app/views/hblog', :output => 'public/views' do
  watch(/^.+(\.haml)/)
end

guard 'sass', :input => 'app/assets/stylesheets/hblog', :output => 'public/css' do
  watch(/^.+(\.scss)/)
end